fn combine(value: u64) -> u64 {
    let result = value

    {
        const value = 5      // shadows the parameter
        result = result + value
    }

    return result
}

db(combine(3))          // 3 + 5
